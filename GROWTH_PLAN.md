# 오디션패스 — 유저 확보 단계 기획 (2026-06-15)

> 결제는 트래픽 확보 후 진행(후순위). 본 문서는 **유저 모으기**에 집중한
> 4개 축의 진단 + 기획: ① SEO ② UX/UI 구조 결함 ③ 오디션 데이터(수집·노출)
> ④ 신청 간편화 + 프로필/포트폴리오 전달.

---

## 1. SEO — 검색 유입 (현재 7/10)

### 잘 되어 있는 것
- `sitemap.ts` 동적 생성(활성 오디션 최대 500건 포함), `robots.ts` 정상
- 루트 메타데이터 우수(title template, 키워드, OG, twitter, GSC 검증)
- **오디션 상세 `layout.tsx`에 `generateMetadata` + JobPosting JSON-LD** → Google Jobs 노출 잠재력 (가장 큰 자산)

### 결함 (임팩트 순)
1. **🔴 카카오톡 공유 OG 이미지 전무** — 한국 유입의 핵심 채널인데 모든 페이지에 `og:image` 없음. 공유 시 미리보기 깨짐 → 바이럴 0.
   - → 루트 정적 OG + 오디션/커뮤니티 상세 동적 OG 이미지(`opengraph-image.tsx`). 오디션은 "제목+주최사+D-day" 카드 자동 생성.
2. **🔴 커뮤니티 = SEO 자산인데 방치** — `/community/[id]` 메타데이터·스키마 0개, 클라이언트 렌더. 후기/꿀팁/질문은 롱테일 검색 유입의 보고인데 색인 안 됨.
   - → 상세 페이지 `generateMetadata` + `Article`(BlogPosting) JSON-LD + sitemap에 커뮤니티 글 포함.
3. **🟡 오디션 상세 본문이 클라이언트 렌더** (`page.tsx`가 `"use client"`). 메타/스키마는 서버라 색인은 되나, 본문 HTML이 비어 색인 속도·품질 손해.
   - → 상세 페이지를 서버 컴포넌트로 전환(본문 SSR), 인터랙션만 클라이언트 분리.
4. **🟡 리스트 페이지 메타데이터 없음** (`/auditions`, `/community`) + 필터별 URL이 루트 메타 공유.
   - → 정적 메타 추가, 카테고리별 랜딩(`/auditions/배우` 등) 검토.
5. **🟢 Organization 스키마, BreadcrumbList 추가**(루트/상세).

### 추가 검색 전략 (콘텐츠)
- 카테고리별 SEO 랜딩 페이지: "배우 오디션 모음", "키즈모델 오디션", "뮤지컬 앙상블 오디션" (14개 카테고리 = 14개 롱테일 진입점)
- 가이드 콘텐츠: "오디션 프로필 사진 잘 찍는 법", "셀프테이프 가이드" → 커뮤니티/블로그로 검색 유입 후 가입 전환

---

## 2. UX/UI 구조 결함

### 사용자 여정
랜딩 → 가입(이메일만) → **프로필(필수)** → 오디션 리스트 → 상세 → 원클릭 지원 → 이력
보조: 커뮤니티(탭), MY(탭)

### 결함 (전환·리텐션 임팩트 순)
1. **🔴 프로필 게이트 타이밍** — 지원 의도가 생긴 순간(상세 페이지 지원 버튼)에 "프로필 먼저 등록" 에러 후 리다이렉트. 의도 최고점에서 마찰 → 이탈.
   - → 가입 직후 온보딩 모달로 프로필 유도, 또는 리스트 상단 "프로필 완성하기(30초)" 배너. `ApplyButton.tsx:53`
2. **🔴 소셜 로그인 없음 (이메일+비번만)** — 한국 유저는 카카오/네이버 로그인 기대. 가입 전환 30~50% 손해 가능.
   - ⚠️ **카카오 로그인은 사업자등록 필요 → 현재 사업자 없어 보류.** 네이버도 사업자 필요 여부 확인 필요.
   - → 사업자등록 전까지는 **구글 OAuth**(사업자 불요) 우선 검토, 이후 카카오/네이버 추가.
3. **🟡 지원 이력이 MY 하위 메뉴에 매몰** — 3탭(홈/커뮤니티/MY)에 "지원" 직접 진입 없음. 핵심 가치(진행 추적)가 안 보임.
   - → 4번째 탭 "지원" 또는 MY 헤더에 "지원 N건" 카드.
4. **🟡 신규 유저 온보딩 가이드 0** — 가입 후 맥락 없이 리스트에 떨어짐. 프로필 필수인 것도 모름.
5. **🟡 마감 임박 긴급성 부족** — D-1 빨간 배지뿐, 행동 유도 없음. → 상세에 "마감 임박! 지금 지원" 스티키 배너(deadline≤3일).
6. **🟢 빠른 수정**: "MY"→"마이/내정보", 로그인 시 로고 클릭 → `/auditions`행, 지원 후 "계속 둘러보기" 링크, 빈 pricing 페이지 정리.
7. **🟢 북마크/찜 기능** — 리텐션용(다음 세션 복귀 유도). 신규 테이블 필요.

---

## 3. 오디션 데이터 — 어디서, 무엇을 보여줄까

### 현재
- 스크래퍼 10개: plfil, megaphone, otr, vaudition, castlink, filmmakers, casting114, castingnara, castik, starlet
- 14개 카테고리 + 3단계 분류기(키워드→규칙→AI)
- `AuditionData`: title, company, genre, deadline, apply_email, description, requirements, source

### 데이터 모델 결함 (노출 품질 직결)
- **🔴 포스터/썸네일 이미지 미수집** — 카드가 전부 텍스트. 시각 매력 0 + OG 이미지 재료 없음.
  - → 스크래퍼에 `image_url` 추가, 카드/상세/OG에 활용.
- **🔴 핵심 필드가 description 텍스트에 묻힘** — `base.py`에 `extract_location`/`extract_phone` 있으나 별도 컬럼 아님. 필터·정렬 불가.
  - → 1급 컬럼 승격: `location`(지역), `pay`(보수/페이), `role`(모집 배역), `gender_req`, `age_min/age_max`, `shoot_date`(촬영/공연일).
- 이 필드들이 생기면 → **지역 필터, 페이 유무 필터, 배역 검색** 등 강력한 탐색 UX 가능.

### 신규 데이터 소스 (확장 후보)
- 연극/뮤지컬: **플레이DB(playdb)**, 대학로 공연 정보
- 영화/독립: 필름메이커스(보유), **인디포스트**, 영화진흥위 공모
- 모델/촬영: 모델 에이전시 채용, 쇼핑몰 피팅 공고
- 방송/엑스트라: 보조출연 에이전시
- **🔴 인스타그램 오디션 공고 계정** — 실제 캐스팅의 상당수가 인스타 DM 모집. 해시태그/계정 모니터링으로 수집(이미 인스타 API 인프라 보유) → 차별화 포인트.
- 공식 제작사/방송사 채용 페이지 직접 크롤

### 보여줄 정보 (카드/상세 개편)
- 카드: 포스터 썸네일 + 카테고리 배지 + **지역 + 페이 유무 + D-day**
- 상세: 모집 배역/조건(성별·나이), 촬영·공연 일정, 보수, 장소, 지원 방법, 원문 링크

---

## 4. 신청 간편화 + 프로필/포트폴리오 전달

### 현재 신청 플로우
- 원클릭 이메일 지원(`api/apply`): 로그인·프로필·중복·일일제한 체크 후 Resend 발송 → 양호
- `apply_type='external'`은 원문 링크만(부가가치 없음)

### 신청 간편화 기획
- **🟡 셀프테이프/자기소개 영상 첨부** — 배우 지원의 핵심. 영상 링크 필드 추가.
- **🟡 지원 시 한 줄 메모(배역/지원 동기)** — 모달에서 선택 입력 → 메일에 포함.
- external 타입도 "원클릭 지원 요청" 가능 사이트는 이메일 자동화 검토.

### 프로필/포트폴리오 전달 — **가장 큰 약점**
현재 지원 메일은 사진을 **"사진 1 보기" 링크**로만 보냄 + 메일 템플릿에 **특기·경력·소속사 누락**(Profile에는 있는데 안 보냄).

1. **🔴 사진을 메일에 인라인 임베드** (`<img>`) — 캐스팅 담당자는 링크 잘 안 누름. 상반신/전신 라벨링하여 본문에 표시.
2. **🔴 누락 필드 메일 포함** — 특기(specialty), 경력(career), 소속사(agency)는 캐스팅에 결정적. 템플릿에 추가. (`application.tsx` props 확장)
3. **🟡 컴프카드(Comp Card) 형식** — 업계 표준. 사진+신체정보+연락처를 한 장 카드/PDF로 첨부 → 전문성·신뢰도 급상승, 차별화.
4. **🟡 메일 제목 개선** — "[오디션 지원] {배역/장르} - {이름}, {나이}/{성별}" 형태로 스캔 용이하게.
5. **🟢 지원 후 상태 추적** — 열람/회신 단계(수동이라도) 추가로 유저 가치↑.

---

## 통합 우선순위 로드맵

### Sprint 1 — 유입·전환 즉효 (이번 주)
- [ ] 카카오/네이버 소셜 로그인 (가입 마찰)
- [ ] 동적 OG 이미지(오디션 상세) + 루트 OG (카톡 공유 바이럴)
- [ ] 지원 메일 개편: **사진 인라인 임베드 + 특기/경력/소속사 포함** (포트폴리오 전달력)
- [ ] 프로필 미완성 배너 / 온보딩 유도 (지원 마찰 제거)

### Sprint 2 — 데이터 품질·탐색 (차주)
- [ ] 스크래퍼에 `image_url` + `location`/`pay`/`role` 컬럼 승격
- [ ] 카드/상세 UI에 포스터·지역·페이·배역 노출
- [ ] 인스타그램 오디션 공고 수집 파이프라인
- [ ] 커뮤니티 상세 SEO(메타+Article 스키마+sitemap)

### Sprint 3 — 구조·리텐션
- [ ] 오디션 상세 SSR 전환(색인 품질)
- [ ] 4번째 탭 "지원" 또는 내활동 IA 개편
- [ ] 카테고리별 SEO 랜딩 페이지 (14개 롱테일)
- [ ] 찜/북마크, 셀프테이프 영상 첨부, 컴프카드

---

## 가장 임팩트 큰 3가지 (단독 추천)
1. **카카오 로그인 + 카톡 공유 OG 이미지** → 한국 시장 유입·가입의 양대 병목 동시 해결
2. **지원 메일 포트폴리오 전달 개편**(사진 임베드 + 누락 필드) → 서비스 핵심 가치의 완성도
3. **포스터 이미지 + 핵심 필드(지역/페이/배역) 수집** → 카드 매력·탐색·OG 재료 동시 확보
</content>
</invoke>
